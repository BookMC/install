pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven {
            url "https://maven.bookmc.org/releases"
        }
    }
}

rootProject.name = "install"

